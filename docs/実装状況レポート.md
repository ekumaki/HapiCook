# ハピクック (HapiCook) 開発進捗レポート

**最終更新日:** 2026/03/04
**ステータス:** Phase 4 (Firebase連携) 完了 → Phase 5 (セキュリティ強化) 着手予定

## 1. プロジェクト概要
WebやSNSのレシピをAI解析で統一管理するReact Native (Expo) アプリ。
仕様書 v1.7 に基づき開発を進行中。

## 2. 実施済みタスク

### Phase 1: 直接入力（手動登録） ✅ 完了
- [x] **レシピ状態管理**: `contexts/RecipeContext.tsx` によるローカルstate管理
- [x] **新規作成画面**: `app/recipe/new.tsx` の実装
- [x] **既存ファイル修正**: `index.tsx`, `[id].tsx`, `edit/[id].tsx`, `_layout.tsx` のContext連携
- [x] **削除機能**: 編集画面最下部への削除ボタン追加

### Phase 2: AI自動解析（Webリンク・テキスト） ✅ 完了
- [x] **Gemini API連携**: `services/geminiService.ts` に解析ロジックを実装
- [x] **テキスト貼り付け画面**: `app/recipe/add-text.tsx` の実装
- [x] **URL入力画面**: `app/recipe/add-link.tsx` の実装（※CORS制限により現在は限定的）
- [x] **プレビュー機能**: `app/recipe/preview.tsx` で解析結果の確認・修正フローを構築
- [x] **ナビゲーション更新**: 各画面間の遷移を統合

### Phase 3: 写真/カメラからの登録 ✅ 完了
- [x] **画像選択フロー**: `app/recipe/add-photo.tsx` からカメラ・アルバム起動
- [x] **画像URI受け渡し**: 取得した画像URIを `preview.tsx` へ渡す
- [x] **Gemini API拡張**: 画像（Base64）をGemini APIで解析する機能を追加（`extractRecipeFromImage`）

### Phase 4: Firebase連携（データ永続化） ✅ 完了
- [x] **Firebaseプロジェクト作成**: `hapicook-14d13`, リージョン: `asia-northeast1`
- [x] **Firebase設定**: `services/firebaseConfig.ts`（環境変数による設定管理、Web/ネイティブ両対応Auth初期化）
- [x] **匿名認証**: アプリ起動時の自動匿名ログイン実装
- [x] **Firestore CRUD**: レシピのCRUD操作をFirestoreに接続、`onSnapshot` によるリアルタイム同期
- [x] **楽観的更新**: Optimistic Update パターンの採用
- [x] **サンプルレシピ**: 初回起動時のサンプルレシピ自動投入（二重投入防止付き）
- [x] **Firebase Storage**: `services/storageService.ts` による画像のアップロード・削除
- [x] **画像永続化**: ローカル画像 → Storage URL への自動更新
- [x] **API更新**: `ImagePicker.MediaTypeOptions`（非推奨）→ `MediaType[]` に更新

### その他のUI改善 ✅ 完了
- [x] **レシピ詳細画面**: 2カラムレスポンシブレイアウト（425px以上で材料・手順を横並び表示）
- [x] **レシピ一覧**: 固定高さカード、2カラムレイアウト、タイトル・タグの省略表示
- [x] **メタデータ表示**: 時間・カロリーの未設定時プレースホルダー（「- 分」「- kcal」）
- [x] **バッジスタイル**: 人数・時間・カロリーのバッジ統一表示
- [x] **設定画面**: 2段階確認のデータ削除、カレンダーナビゲーション改善
- [x] **モーダル**: レイアウト修正（viewport-fit=cover対応）

## 3. 実装済みコンポーネント・ファイル

| ファイルパス | 内容 |
|:---|:---|
| `app/(tabs)/index.tsx` | レシピ一覧画面（メイン） |
| `app/(tabs)/settings.tsx` | 設定画面 |
| `app/(tabs)/_layout.tsx` | タブナビゲーション |
| `app/recipe/[id].tsx` | レシピ詳細画面 |
| `app/recipe/edit/[id].tsx` | レシピ編集画面 |
| `app/recipe/new.tsx` | 手動レシピ新規作成画面 |
| `app/recipe/add-text.tsx` | テキスト入力AI解析画面 |
| `app/recipe/add-link.tsx` | URL入力AI解析画面 |
| `app/recipe/add-photo.tsx` | 写真/カメラAI解析画面 |
| `app/recipe/preview.tsx` | AI解析結果プレビュー画面 |
| `app/_layout.tsx` | ルートレイアウト |
| `components/recipe/RecipeCard.tsx` | レシピ表示用カード |
| `components/modals/AddMenuModal.tsx` | レシピ登録方法選択モーダル |
| `constants/Colors.ts` | アプリ全体のカラーテーマ（オレンジベース） |
| `contexts/RecipeContext.tsx` | レシピCRUD + Firebase連携のContext |
| `services/firebaseConfig.ts` | Firebase初期化設定 |
| `services/geminiService.ts` | Gemini API連携（テキスト・画像解析） |
| `services/storageService.ts` | Firebase Storage画像アップロード・削除 |
| `types/recipe.ts` | レシピ・ユーザー情報の型定義 |
| `data/sampleRecipes.ts` | 初回起動用サンプルレシピデータ |

## 4. 今後の開発タスク (Next Steps)

1. **Phase 5: セキュリティ強化 🟠 次に着手**
   - Firestore セキュリティルール（ユーザー別アクセス制御）
   - Storage セキュリティルール（フォルダ制限、ファイルサイズ・形式制限）
   - API キーの使用制限設定（Google Cloud Console）

2. **Phase 6: 会員システム・課金 🟣 将来**
   - Google Sign-In の実装
   - レシピ登録数・AI解析回数の制限チェック
   - RevenueCat (サブスクリプション) + AdMob (広告)

3. **Phase 7: UX改善・追加機能 🟣 将来**
   - Cooking Mode（常時点灯）の改善
   - レシピの検索・フィルタリング機能
   - Share Extension（iOS/Android）

## 5. 技術的な既知の課題

| 課題 | 影響 | 優先度 | 備考 |
| :--- | :--- | :--- | :--- |
| Firestore/Storageがテストモード | セキュリティリスク | **高** | Phase 5 で対応予定 |
| Webリンク取り込みの制限（CORS） | Web版でURL取り込みが困難 | 中 | Cloud Functions等で解決可能 |
| `shadow*` StyleSheet 非推奨警告 | 動作に影響なし | 低 | React Native Web側の問題 |
| `getReactNativePersistence` 型エラー | ビルド・動作に影響なし | 低 | TypeScript定義の問題のみ |

## 6. 起動・確認方法
```bash
# Expo開発サーバー起動
npx expo start

# Webブラウザで確認
npm run web

# リモートリポジトリ
# https://github.com/ekumaki/HapiCook
```
---
*本ドキュメントは AI アシスタントによって自動生成・更新されました。*
